<p><%= t 'mailer.unlock_instructions.hello', @resource.email %>!</p>

<p><%= t 'mailer.unlock_instructions.locked' %></p>

<p><%= t 'mailer.unlock_instructions.link_unlock' %></p>

<p><%= link_to (t 'mailer.unlock_instructions.unlock'), unlock_url(@resource, unlock_token: @token) %></p>
